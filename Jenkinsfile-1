pipeline {
  agent any
  options {
    buildDiscarder(logRotator(numToKeepStr: '5'))
  }
  stages {
    stage('Build') {
      steps {
        sh 'sudo docker build -t test9877/java-web-app:latest .'
      }
    }
    stage('Scan') {
      steps {
        sh 'trivy test9877/java-web-app:latest'
      }
    }
  }
}
